const csrf = document.querySelector('meta[name="csrf-token"]')?.content || '';
const navs = [...document.querySelectorAll('[data-view]')];
const views = [...document.querySelectorAll('.view')];
function showView(name){views.forEach(v=>v.classList.toggle('active',v.id===name));navs.forEach(n=>n.classList.toggle('active',n.dataset.view===name));history.replaceState(null,'','#'+name);window.scrollTo({top:0,behavior:'smooth'});}
navs.forEach(n=>n.addEventListener('click',()=>showView(n.dataset.view)));
document.querySelectorAll('[data-go]').forEach(b=>b.addEventListener('click',()=>showView(b.dataset.go)));
const initial=location.hash.replace('#',''); if(initial && document.getElementById(initial)) showView(initial);
document.querySelectorAll('.toggle-edit').forEach(btn=>btn.addEventListener('click',()=>btn.closest('.item')?.querySelector('.edit-box')?.classList.toggle('hidden')));
document.querySelectorAll('[data-confirm]').forEach(btn=>btn.addEventListener('click',e=>{if(!confirm(btn.dataset.confirm))e.preventDefault();}));
const diagBtn=document.getElementById('diagnose-btn');
if(diagBtn){diagBtn.addEventListener('click',async()=>{const truck=document.getElementById('diag-truck').value;const code=document.getElementById('diag-code').value;const symptoms=document.getElementById('diag-symptoms').value.trim();const status=document.getElementById('diagnose-status');const answer=document.getElementById('diagnose-answer');if(!symptoms){status.textContent='Beskriv symptomerne først.';return;}diagBtn.disabled=true;status.textContent='TruckOS analyserer…';answer.classList.add('hidden');try{const r=await fetch('/api/diagnose',{method:'POST',headers:{'Content-Type':'application/json','X-CSRF-Token':csrf},body:JSON.stringify({truck_id:truck,fault_code:code,symptoms})});const data=await r.json();if(!r.ok)throw new Error(data.error||'Fejl');answer.querySelector('pre').textContent=data.answer;const sf=document.getElementById('solution-form');if(sf&&data.diagnosis_id)sf.action='/solutions/'+data.diagnosis_id;answer.classList.remove('hidden');status.textContent=data.ai?'AI-vurdering klar.':'Lokal vurdering klar.';}catch(e){status.textContent='Fejl: '+e.message;}finally{diagBtn.disabled=false;}});}
