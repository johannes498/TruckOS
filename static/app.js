const navs=[...document.querySelectorAll('.nav[data-view]')],views=[...document.querySelectorAll('.view')];
function show(id){views.forEach(v=>v.classList.toggle('active',v.id===id));navs.forEach(n=>n.classList.toggle('active',n.dataset.view===id));history.replaceState(null,'','#'+id)}
navs.forEach(n=>n.addEventListener('click',()=>show(n.dataset.view))); const wanted=location.hash.slice(1); if(wanted&&document.getElementById(wanted)) show(wanted);
const df=document.getElementById('diagnoseForm'); if(df)df.addEventListener('submit',async e=>{e.preventDefault();const out=document.getElementById('diagnosisAnswer');out.textContent='TruckOS analyserer…';const body=Object.fromEntries(new FormData(df));try{const r=await fetch('/api/diagnose',{method:'POST',headers:{'Content-Type':'application/json','X-CSRF-Token':document.querySelector('input[name=csrf_token]')?.value||''},body:JSON.stringify(body)});const d=await r.json();out.textContent=d.answer||d.error||'Ingen svar';}catch{out.textContent='Kunne ikke kontakte TruckOS.'}});
const gps=document.getElementById('assistGps'); if(gps)gps.onclick=()=>navigator.geolocation?navigator.geolocation.getCurrentPosition(p=>{document.getElementById('assistLat').value=p.coords.latitude;document.getElementById('assistLng').value=p.coords.longitude;gps.textContent='✅ GPS gemt';},()=>{gps.textContent='GPS kunne ikke hentes'}):null;
